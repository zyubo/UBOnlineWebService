@model IEnumerable<UBOnlineWebApiTest2.Models.Read>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.annId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.stuName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.read)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.annId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.stuName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.read)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.readId }) |
            @Html.ActionLink("Details", "Details", new { id=item.readId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.readId })
        </td>
    </tr>
}

</table>
