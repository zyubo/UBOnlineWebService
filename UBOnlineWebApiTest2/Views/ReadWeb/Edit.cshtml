@model UBOnlineWebApiTest2.Models.Read

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Read</legend>

        @Html.HiddenFor(model => model.readId)

        <div class="editor-label">
            @Html.LabelFor(model => model.annId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.annId)
            @Html.ValidationMessageFor(model => model.annId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.stuName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.stuName)
            @Html.ValidationMessageFor(model => model.stuName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.read)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.read)
            @Html.ValidationMessageFor(model => model.read)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
